name: Build Padavan for DIR-882

on:
  release:
    types: [published]
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      kernel:
        description: 'Linux Kernel version'
        required: true
        default: '3.4'
        type: choice
        options: ['3.4', '4.4']

jobs:
  build:
    runs-on: ubuntu-20.04
    if: github.event.repository.owner.id == github.event.sender.id

    steps:
      - name: Checkout
        uses: actions/checkout@master

      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get -y install unzip libtool-bin curl cmake gperf gawk flex bison nano xxd fakeroot \
          cpio git python-docutils gettext automake autopoint texinfo build-essential help2man \
          pkg-config zlib1g-dev libgmp3-dev libmpc-dev libmpfr-dev libncurses5-dev libltdl-dev wget

      - name: Clone source code
        run: |
          if [ "${{ github.event.inputs.kernel }}" = "4.4" ]; then
            git clone --depth=1 https://github.com/Kool-Shine/padavan-4.4.git /opt/rt-n56u
          else
            git clone --depth=1 https://github.com/Kool-Shine/rt-n56u.git /opt/rt-n56u
          fi
          cd /opt/rt-n56u/toolchain-mipsel
          sh dl_toolchain.sh
          mkdir -p /opt/images/

      - name: Build firmware
        env:
          TNAME: DIR-882
        run: |
          cd /opt/rt-n56u/trunk
          if [ ! -f configs/templates/$TNAME.config ]; then
            echo "configs/templates/$TNAME.config not found"
            exit 1
          fi
          cp -f configs/templates/$TNAME.config .config
          sed -i 's/CONFIG_FIRMWARE_INCLUDE_OPENSSL_EXE=n/CONFIG_FIRMWARE_INCLUDE_OPENSSL_EXE=y/g' .config
          sudo ./clear_tree
          sudo ./build_firmware_modify $TNAME 0
          sudo mv -f images/*.trx /opt/images/

      - name: Upload packages
        uses: actions/upload-artifact@master
        if: always()
        with:
          name: Padavan-DIR-882-${{ github.event.inputs.kernel || '3.4' }}
          path: /opt/images/
